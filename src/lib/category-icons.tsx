import { Camera, Aperture, Lightbulb, Mic, Plane, Speaker, type LucideIcon } from "lucide-react";

export const categoryIconMap: Record<string, LucideIcon> = {
  camera: Camera,
  aperture: Aperture,
  lightbulb: Lightbulb,
  mic: Mic,
  plane: Plane,
  speaker: Speaker,
};
